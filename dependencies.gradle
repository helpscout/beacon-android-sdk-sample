ext {
    // Sdk and tools
    minSdkVersion = 21
    targetSdkVersion = 34
    compileSdkVersion = 34

    gradlePluginVersion = '8.8.0'
    kotlinVersion = '2.1.10'

    // Testing
    jUnitVersion = '4.13.2'

    //debug logging library
    timberVersion = '5.0.1'

    // Android libraries
    materialVersion = '1.12.0'
    constraintLayoutVersion = '2.2.0'

    // Beacon Note: We recommend to always use the latest version of the SDK. See changelog.md for changes.
    beaconVersion = '+'
}
