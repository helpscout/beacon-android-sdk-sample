ext {
    // Sdk and tools
    minSdkVersion = 21
    targetSdkVersion = 32
    compileSdkVersion = 32

    gradlePluginVersion = '7.0.4'
    kotlinVersion = '1.5.30'

    // Testing
    jUnitVersion = '4.13.1'

    //debug logging library
    timberVersion = '5.0.1'

    // Android libraries
    materialVersion = '1.4.0'
    constraintLayoutVersion = '2.1.1'

    // Beacon Note: We recommend to always use the latest version of the SDK. See changelog.md for changes.
    beaconVersion = '4.0.0'
}
