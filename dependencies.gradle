ext {
    // Sdk and tools
    minSdkVersion = 21
    targetSdkVersion = 30
    compileSdkVersion = 30

    gradlePluginVersion = '4.2.1'
    kotlinVersion = '1.5.10'
    // this is test customer issue
    koinVersion = '3.1.0'

    // Testing
    jUnitVersion = '4.13.1'

    //debug logging library
    timberVersion = '4.7.1'

    // Android libraries
    materialVersion = '1.3.0'
    constraintLayoutVersion = '2.0.4'

    // Beacon Note: We recommend to always use the latest version of the SDK. See changelog.md for changes.
    beaconVersion = '3.0.2'
}
